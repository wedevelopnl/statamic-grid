title: Grid
fields:
  -
    handle: grid
    field:
      type: replicator
      display: Grid
      instructions: 'Build your page by using various predefined grid elements.'
      collapse: true
      sets:
        new_set_group:
          display: 'New Set Group'
          sets:
            section:
              display: Section
              fields:
                -
                  import: statamic-grid::section
